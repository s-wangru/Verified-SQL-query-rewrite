365: select d.name as department, e.name as employee, e.salary as salary from employee as e join department as d on e.departmentid = d.id join (select departmentid, max(salary) as m_salary from employee group by departmentid) as t1 on t1.departmentid = e.departmentid where e.salary = t1.m_salary;
126: select d.name as department, e.name as employee, e.salary from department as d join (select em.name, em.salary, em.departmentid from employee as em join (select max(salary) as salary, departmentid from employee group by departmentid) as m on (em.salary = m.salary and em.departmentid = m.departmentid)) as e on d.id = e.departmentid;
130: select d.name as department, e1.name as employee, e2.salary from employee as e1 join department as d on e1.departmentid = d.id join (select departmentid, max(salary) as salary from employee group by departmentid) as e2 on e1.departmentid = e2.departmentid where e1.salary = e2.salary;
330: select b.name as employee, a.salary, c.name as department from (select departmentid, max(salary) as salary from employee group by departmentid) as a join employee as b on (a.departmentid = b.departmentid and a.salary = b.salary) join department as c on a.departmentid = c.id;
455: select department.name as department, b.name as employee, salary as salary from department join (select a.departmentid, a.salary, b.name from (select departmentid, max(salary) as salary from employee group by departmentid) as a join employee as b on (a.salary = b.salary and a.departmentid = b.departmentid)) as b on department.id = b.departmentid;
480: select tb3.name as department, tb4.name as employee, tb2.maxsalary as salary from (select tb1.departmentid, max(tb1.salary) as maxsalary from employee as tb1 group by tb1.departmentid) as tb2 join employee as tb4 on (tb2.maxsalary = tb4.salary and tb2.departmentid = tb4.departmentid) join department as tb3 on tb2.departmentid = tb3.id;
160: select dep.department_name as department, e.name as employee, salary from employee as e join (select d.id as department_id, d.name as department_name, max(salary) as max_salary from employee as e join department as d on e.departmentid = d.id group by d.id) as dep on e.departmentid = dep.department_id where e.salary = dep.max_salary;
171: select t.department, emp.name as employee, t.salary from (select d.name as department, e.departmentid, max(e.salary) as salary from employee as e join department as d on e.departmentid = d.id group by 1, 2) as t left outer join employee as emp on (emp.departmentid = t.departmentid and emp.salary = t.salary);
376: select d.name as department, e.name as employee, e.salary from employee as e join department as d on d.id = e.departmentid join (select d.id, max(e.salary) as salary from employee as e join department as d on d.id = e.departmentid group by d.id) as t on (e.departmentid = t.id and e.salary = t.salary);
442: select dd.name as department, ee.name as employee, ee.salary as salary from employee as ee join (select d.id, d.name, max(e.salary) as salary from employee as e join department as d on d.id = e.departmentid group by d.id) as dd on dd.id = ee.departmentid where ee.salary = dd.salary;
30: select department, employee, salary from (select d.name as department, e.name as employee, e.salary as salary, max(e.salary) over (partition by e.departmentid) as maxsalary from employee as e join department as d on e.departmentid = d.id) as max_salary where maxsalary = salary order by salary;
118: select d.name as department, e1.name as employee, e1.salary from department as d join employee as e1 on d.id = e1.departmentid join (select e2.departmentid, max(e2.salary) as salary from employee as e2 group by e2.departmentid) as t on (e1.departmentid = t.departmentid and e1.salary = t.salary);
56: select department.name as department, temp.employee, temp.salary from department join (select temp.departmentid, employee.name as employee, employee.salary as salary from employee join (select max(salary) as maxsalary, departmentid from employee group by departmentid) as temp on (employee.salary = temp.maxsalary and employee.departmentid = temp.departmentid)) as temp on temp.departmentid = department.id;
277: select department.name as department, employee.name as employee, employee.salary as salary from employee join (select departmentid, max(salary) as max_salary from employee group by departmentid) as temp on (employee.departmentid = temp.departmentid and employee.salary = temp.max_salary) join department on department.id = employee.departmentid;
428: select d.name as department, e.name as employee, e.salary from employee as e join department as d on e.departmentid = d.id join (select max(salary) as salary, departmentid from employee group by departmentid) as max on (e.salary = max.salary and e.departmentid = max.departmentid);
361: select d.name as department, e.name as employee, salary from employee as e left outer join department as d on e.departmentid = d.id left outer join (select max(salary) as maxsalary, departmentid from employee group by departmentid) as a on d.id = a.departmentid where e.salary = a.maxsalary;
