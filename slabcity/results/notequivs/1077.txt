149: select p.project_id, e.employee_id from project as p left outer join employee as e on p.employee_id = e.employee_id right outer join (select a.project_id, max(b.experience_years) as max from project as a left outer join employee as b on a.employee_id = b.employee_id group by 1) as c on (p.project_id = c.project_id and e.experience_years = c.max);
1: select distinct a.project_id, a.employee_id from (select project_id, project.employee_id, experience_years, max(experience_years) over (partition by project_id) as max_years from project join employee on project.employee_id = employee.employee_id) as a where a.experience_years = a.max_years;
293: select t1.project_id, t1.employee_id from project as t1 join employee as t2 on t2.employee_id = t1.employee_id join (select t1.project_id, max(t2.experience_years) as max_years from project as t1 join employee as t2 on t2.employee_id = t1.employee_id group by t1.project_id) as t3 on t3.project_id = t1.project_id where t2.experience_years = t3.max_years;
274: select t.project_id, e.employee_id from employee as e join (select project_id, max(experience_years) as max from project as p join employee as e on p.employee_id = e.employee_id group by project_id) as t on e.experience_years = t.max join project as p on (p.project_id = t.project_id and e.employee_id = p.employee_id);
59: select t1.project_id, t2.employee_id from (select p.project_id, max(e.experience_years) as most_experienced from project as p join employee as e on p.employee_id = e.employee_id group by 1) as t1 join (select p.project_id, p.employee_id, e.experience_years from project as p join employee as e on p.employee_id = e.employee_id) as t2 on (t1.project_id = t2.project_id and t1.most_experienced = t2.experience_years);
